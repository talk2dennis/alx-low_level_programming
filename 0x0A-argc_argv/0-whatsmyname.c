#include <stdio.h>

/**
 * main - function that prints out the name of the program
 * @argc: int argc
 * @argv: string array argurement
 * Return: always (0)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
