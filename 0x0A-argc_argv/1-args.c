#include <stdio.h>

/**
 * main - function that prints out the number of argurement
 * @argc: int argc
 * @argv: string array argurement
 * Return: always (0)
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc);
	return (0);
}
