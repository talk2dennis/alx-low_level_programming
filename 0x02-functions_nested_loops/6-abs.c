#include "main.h"

/**
 * _abs(int) - a function that returns the abs  of a number
 * @n: is an int argurement
 * Return: returns and int
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-(n);
	}
	else
	{
		return (n);
	}
}
