#include <string.h>
#include "main.h"

/**
 * main - the entrance point to our program that prints a string
 * Return: Always 0 on success
 */

int _putchar(char c)
{
	int i, n;

	printf("%c\n", c);
	return (0);
}
